## Eye Care 1.3.2 版本说明

> 本版本是在 1.3.1 基础上的一次小版本升级，重点是：**让「浏览器统计」的网站图标抓取变得更克制、更透明**。
>
> 起因是有用户发现这个本该离线运行的工具会产生网络连接。排查确认：除了你手动点「检查更新」，唯一的联网来源就是**为网站卡片抓取图标**（favicon，也就是浏览器标签页上的那个小图标）。功能本身没问题，但抓取的**时机**和**次数**都不够克制，本次做了系统性收敛，并把这件事在设置里明确告知。
>
> 日常使用体验与原来保持一致，统计数据不受任何影响。

### 主要改进

- **抓取时机：改成「你正在访问该网站时」**
  - 以前是等你打开「网站」统计页时才去抓图标 —— 那时你早就离开那个网站了，对方服务器的日志上会多出一次孤零零的回访记录。
  - 现在改成在你**实际停留在该网站期间**抓取，请求和你自己的浏览行为同时发生。
  - 对你来说的直接变化：**打开「网站」统计页、翻站点详情，现在完全不产生任何网络请求。**

- **抓取次数：从「每个访问过的网站都抓」收敛到「只抓你真正在用的」**
  - **满 1 分钟才抓**：误点开、看两眼就关掉的网站，现在一个请求都不会产生。
  - **子域名不重复抓**：`space.bilibili.com`、`t.bilibili.com` 等共用主域名 `bilibili.com` 的一次抓取，时长也合并计算。
  - **抓过就不再抓**：每个网站成功抓取一次后永久使用本地缓存，不会重复请求。
  - 全程直接向该网站请求，**不经过任何第三方服务器**，也不会上传你的任何信息。

- **新增：可以清除单个网站的图标缓存**
  - 位置：**应用设置 → 网站 → 点进某个站点 → 左下角「清除图标缓存」**。
  - 用途：网站换了新 logo（图标以前抓到后就永久缓存，不会自动更新），或者抓到的图标不对。
  - 清除后图标先变回域名首字母，下次你访问该网站满 1 分钟时会重新抓取。
  - 对于之前抓取失败的网站，这个操作也能让它立刻重试，不必再等冷却时间。

- **新增：设置里明确告知会联网**
  - 「记录浏览器数据（仅域名）」开关旁多了一个 ⓘ 标记，鼠标移上去会在光标旁显示说明气泡，写清楚这个功能会联网做什么、什么时候做、抓多少次。
  - 这个开关**默认仍是关闭的**。不开启浏览器统计，程序除了你手动检查更新之外不会有任何联网行为。

### 问题修复

- 修复了后台会持续进行不必要磁盘读取的问题：图标已缓存的网站，每浏览一秒就会检查一次缓存文件是否存在。现已改为只检查一次并记住结果。
- 修复了程序退出过程中，有极小概率仍会发出一次图标请求的问题。

### 关于隐私（未改变的部分）

以下几点自浏览器统计功能引入以来就是如此，本次没有任何放宽，一并说明：

- 只记录**网站域名**（如 `bilibili.com`），**不记录**完整网址、页面标题、搜索词或页面内容。
- 所有统计数据只存在你本机的 `user_data` 目录，不会上传到任何地方。
- 「记录浏览器数据」开关默认关闭，需要你主动开启。关闭后已有数据仍会保留。

### 升级与兼容性

- **升级方式**：直接覆盖安装即可，无需卸载旧版本。
- **数据与配置**：与之前版本完全兼容，使用数据和个人设置都会自动保留。
- **已缓存的图标**：保留在 `user_data/domain_icons/` 目录，升级后继续可用，不会重新抓取。
- **面向的用户**：推荐所有 1.3.x 用户升级，尤其是开启了「记录浏览器数据」、或关注程序网络行为的用户。
