# EyE Care Release Notes

## v1.1 — 2026-06-20

### 重要变更：界面引擎升级

本版本将 UI 从 Chromium（QWebEngine）内核**全面切换为 Qt Quick 原生渲染**。
对用户最直观的变化是**内存占用大幅下降**——以往切几个页面后程序会爬到 2–4 GB，现在稳定在 50–150 MB 以内。

### 新功能 / 改进

- **原生界面**：主仪表盘、设置、应用管理、黑名单、日历、更新检查等全部页面均为原生 QML 渲染，动画更流畅，CPU 占用更低
- **多显示器修复**：休息提醒气泡现在会跟随鼠标所在屏幕弹出，不再固定在主屏
- **无边框窗口**：支持拖拽四边四角缩放；标题栏拖动移动；最大化/恢复
- **毛玻璃效果**：休息提醒气泡与全屏休息遮罩应用 Windows Acrylic 半透明效果
- **跨栏高亮联动**：鼠标悬停饼图/柱状图/应用列表任意一处，另两处同步高亮对应应用
- **日历范围选择**：日历选择器支持选择连续日期范围，统计区间更灵活
- **星空背景**：主界面背景加入随机星点（大星闪烁动画）

### Bug 修复

- **修复：改完设置后通知延迟约 20 分钟才弹**
  根因：切换休息间隔后内部「下次提醒阈值」未被重置，导致提醒被卡住直到连续用眼时长追上旧阈值。现在修改设置后立即生效。

- **修复：关闭「前台自动勿扰」程序后不再弹休息提醒**
  根因：勿扰期间积累的「已通知」状态在 App 关闭后未清除，导致用户离开勿扰 App 后仍需等满一整个提醒间隔。现在退出勿扰后会立即重新判断是否需要提醒。

- **修复：有时听得见提醒声，但看不见弹窗**
  根因：通知气泡在程序启动时固定定位到主屏，双显示器环境下若用户在副屏工作，气泡出现在主屏（视线外）。现在每次弹出前重新定位到鼠标所在屏幕。

### 注意事项

- 数据与配置（`user_data\` 目录）与旧版完全兼容，无需迁移。
- 旧版的后台 Web 调试台（`/api/debug/console`）已随本次迁移一并移除。

---

## v1.0

初始发布版本。基于 QWebEngine + Flask 构建。
