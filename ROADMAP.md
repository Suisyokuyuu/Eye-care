# 产品路线图

本文档归并自原 `next_version_features.md`，记录规划中的下版本功能。

---

## 1. 立即休息按钮保护期禁用

### 背景

当前点击「立即休息」后有保护时间（防止频繁触发），但 UI 上没有体现，用户可能连续点击导致意外行为。

### 需求描述

#### 1.1 立即休息按钮保护期禁用

- 点击「立即休息」后，进入保护期（时长可配置，默认参考现有保护逻辑）
- 保护期内，按钮变灰（disabled 状态），禁止点击
- 保护期结束后，按钮恢复可点击状态

#### 1.2 稍后/跳过按钮动画期间禁用

- rest 遮罩渐入动画期间（从显示到完全展示），禁用「稍后」/「跳过」按钮
- 渐入动画完成后，启用按钮
- 防止用户在动画过程中误触导致界面闪烁

### 技术要点

- **按钮状态管理**：前端维护 `restButtonDisabled` 等状态，点击后禁用，保护期结束恢复
- **后端事件通知**：rest_start 成功后通过 WebSocket 或轮询通知前端进入保护期，或前端按点击时间计算保护期
- **渐入动画检测**：CSS `animationend`/`transitionend` 或固定延迟（如 300ms）与动画时长匹配

### 验收点

1. 点击立即休息后，按钮立即变灰
2. 保护期内再次点击无效
3. 保护期结束后按钮恢复正常
4. rest 遮罩出现时，稍后按钮在动画期间不可点击
5. 动画结束后按钮可点击

---

## 2. 新手引导页

### 背景

首次启动应用时，用户可能对功能不了解，需要引导其完成基础配置和使用。

### 需求描述

- 首次启动时显示引导页（可跳过）
- 引导页展示核心功能：休息提醒设置、勿扰模式、统计查看、托盘图标功能
- 引导完成或跳过后，记录标志位，后续启动不再显示
- 支持在设置中「重置引导」

### 技术要点

- **引导状态存储**：使用配置文件或本地存储记录 `has_seen_guide` 标志
- **引导页面设计**：单页多步骤或滑动卡片式，简洁图文说明，「下一步」/「跳过」/「完成」按钮
- **功能入口**：设置页面添加「重新显示引导」选项

### 验收点

1. 首次启动显示引导页
2. 引导可正常完成或跳过
3. 再次启动不再自动显示
4. 设置中可手动触发重新显示

---

## 3. Notify 样式应用超时稳定性 & 自动化测试

### 背景

部分环境下，日志中多次出现 `DIAG_NOTIFY_STYLE_APPLY_FAIL`，提示在约 1.2s 窗口内等待 notify controller 就绪超时。
目前用户实际看到的 notify 弹窗样式和交互均正常，但该现象提示底层时序仍存在不稳定风险，未来可能在压力场景下
演化为「有声音但无窗口 / 样式错乱」等用户可感知问题。

### 需求描述

- 当前版本：保持行为不变，本问题暂不在本版本修复，只做监控与记录
- 下一个版本：
  - 为 notify 弹窗样式应用流程补充自动化测试（含时序/超时场景）
  - 明确 controller 就绪的正确判定条件与重试策略
  - 在极端超时/失败场景下，保证至少有「退化但可见」的 UI 呈现（避免完全无窗）

### 技术要点

- **自动化测试补充**：
  - 为 notify 样式应用链路设计单元测试 / 集成测试
  - 模拟 controller 延迟就绪、永不就绪等场景，验证日志与降级行为
- **时序与重试策略**：
  - 明确等待 controller ready 的最大等待时长与重试间隔
  - 避免在 UI 成功渲染情况下仍持续打出误导性的 `STYLE_APPLY_FAIL` 日志
- **降级策略**：
  - 明确在样式应用失败时的 UI 兜底样式（例如基础系统样式 / 简化布局）

### 验收点

1. 当前版本在用户侧 notify 仍保持现有样式与交互，无感知行为变化
2. 自动化测试中能稳定复现 controller 延迟/超时场景，并有明确断言
3. 时序与重试策略在文档中有说明，并在代码中实现
4. 在模拟极端失败场景下，仍可看到退化但可用的 notify 弹窗，不出现「完全不弹」的情况