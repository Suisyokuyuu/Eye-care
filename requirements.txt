# Eye Care - Requirements for PyInstaller packaging

# YAML parsing
PyYAML==6.0.3

# Qt host (PySide6 / Qt Quick) — 6.11.1 为 Python 3.14 兼容版本。
# UI 已全面迁至 QML/Qt Quick 原生外壳（不再使用 QWebEngine / Flask / web SPA）；
# 用到 QtQuick.Controls.Basic / Shapes / Effects(MultiEffect)。
# QML 插件随此 wheel 一并安装，无需额外包；打包收集见 EyE Care.spec。
PySide6==6.11.1

# System monitoring (foreground/idle 探针)
psutil==7.2.2

# Windows API wrappers
pywin32==311

# PyInstaller (for packaging)
pyinstaller==6.19.0
pyinstaller-hooks-contrib==2026.1
