# EyE Care 使用手册

EyE Care 是一款 Windows 桌面**护眼 + 应用使用时长统计**工具。它在后台记录各应用的前台使用时间，按设定的节奏提醒你起身休息，并提供清晰的使用统计面板。

> 本手册面向最终用户。开发/构建相关请见 `docs/ARCHITECTURE.md` 等技术文档。

---

## 一、安装与启动

### 直接使用打包版
1. 解压发行包，得到 `EyE Care` 文件夹。
2. 双击 **`EyE Care.exe`** 启动（已内置运行环境，无需另装 Python 或依赖）。
3. 首次运行会在程序目录下自动创建 `user_data\`（存放你的统计数据与配置）。

### 从源码运行（开发者）
- 安装依赖：`scripts\install_deps.bat`
- 启动：双击 **`scripts\run_qml_shell.bat`**（QML 原生界面 + 调试日志，控制台可见）

---

## 二、主界面

启动后是单窗口仪表盘，分左右两栏。

### 左栏 · 应用使用统计
- **甜甜圈饼图**：各应用/分类的使用占比；中心显示总时长。鼠标悬停某块会高亮联动右栏。
- **应用 / 分类 / 浏览器** 切换：按单个应用看、按分类汇总，或按**网站域名**统计浏览器内的使用时长。
  - 「浏览器」页签默认隐藏，需在设置中开启**「记录浏览器数据（仅域名）」**后才会出现并开始记录（见第六节）。
  - 域名条目显示网站图标（favicon，自动获取并缓存；获取不到时显示首字母圆标）。**点击某个域名卡片**可打开该**站点详情设置**（改显示名、勾选子站点独立统计，见下）。
  - 子域名默认**合并到主域名**统计（如 `space.bilibili.com` 计入 `bilibili.com`）。可在站点详情里把某个子站点勾选为**独立统计**——默认已预置 `drive.google.com`、`photos.google.com`、`mail.google.com` 三个独立站点。合并规则的任何改动都会**立即回溯**全部历史数据（底层始终按完整子域名记录）。
- **日 / 周 / 月** 切换：统计区间。
- **日期导航 ◀ ▶**：切换到上一天 / 下一天；中间的**日历按钮**可打开日期选择器，支持选**单日**或**日期范围**。
- **应用列表**：每行显示应用图标、名称、时长、占比条。
  - **点击某张卡片** → 打开该应用的**详情页**（使用时间段柱状图 + 路径 + 设置）。
  - 在「分类」视图下点击分类 → 打开分类详情，列出该分类下的应用。

### 右栏 · 屏幕时间与休息
- **屏幕时间统计**：按时段堆叠的柱状图 + Top 榜。图表维度**跟随左栏视图**——左栏切到「分类」或「浏览器」时，柱状图与 Top 榜同步按分类/域名展示；下方指标卡始终显示全局数据。
- **指标卡**：已连续用眼、今日屏幕时间、休息完成率、最长连续专注、提醒频率、休息时长、今日休息/跳过。
- **立刻休息**：立即进入一次休息（全屏休息覆盖层）。

---

## 三、顶部工具栏

| 按钮 | 功能 |
|------|------|
| **设置** | 打开设置面板（见第六节） |
| **导入** | 导入「数据」或「设置」（选择文件） |
| **导出** | 导出「数据」（zip）或「设置」（json） |
| **应用设置** | 已记录应用列表，可搜索；点卡片进入应用详情编辑分类/显示名/前台自动勿扰，或**排除计时**。开启浏览器统计后顶部出现**「应用 / 网站」**页签：网站页签列出各站点，点击进入**站点详情**（编辑显示名、勾选子站点独立统计） |
| **黑名单** | 查看已排除计时的应用，可逐个移除恢复记录（历史数据不恢复） |
| **检查更新** | 查看自动更新状态；新版下载完成后可立即重启升级 |

窗口右上角为 **最小化 / 最大化 / 关闭**；窗口四边四角可拖动缩放，标题栏可拖动移动。

---

## 四、休息提醒

- 当连续用眼达到设定阈值，右下角弹出**休息提醒浮层**（可「立刻休息」或「跳过本轮」），并可选播放提示音。关闭、自动消失或跳过后，会从当时起重新等待一个完整提醒间隔，再进入下一轮提醒。
- 进入休息后显示**全屏休息覆盖层**并倒计时；到点可选播放结束提示音。
- 休息时长、提醒间隔、提示音开关均可在设置中调整。

---

## 五、系统托盘

关闭主窗口时（若设置为「最小化到托盘」）程序继续在后台计时。右下角托盘图标：

- **双击** → 显示主界面
- **右键菜单**：显示主界面、立即休息、运行模式（正常 / 勿扰 / 离开）、打开设置、检查更新、打开数据目录、退出
- 图标右下角的小圆点颜色表示当前模式（绿=正常 / 红=勿扰 / 灰=离开 / 浅灰=自动暂离）。自动切换后图标会即时同步。

程序启动约 5 秒后会静默检查更新，之后每 6 小时检查一次。发现新版后会在后台下载，
并依次校验整个 ZIP 和包内文件；准备完成时通过托盘提示。点击「检查更新」→「立即重启升级」，
程序会先正常保存计时数据，再退出并由独立升级器替换文件、重启新版。升级不会覆盖
`user_data`；若替换失败，会恢复旧文件并重新启动原版本。

---

## 六、设置项说明

| 设置 | 说明 |
|------|------|
| 开机自启动 | 登录 Windows 时自动启动 |
| 启动时进入勿扰 | 启动即勿扰模式 |
| 启动时显示主界面 | 否则启动后仅驻留托盘 |
| 无操作暂停阈值 | 多少秒无鼠标键盘视为离开，暂停计时；自动勿扰中的游戏/视频仍按屏幕活动计时 |
| 关闭按钮行为 | 询问 / 最小化到托盘 / 直接退出 |
| 启用休息提醒 | 总开关 |
| 休息提醒间隔 | 连续用眼多久后提醒（分钟） |
| 休息时长 | 每次休息时长（秒 / 分） |
| 通知显示时长 | 提醒浮层自动消失的秒数；设为 0 时不自动隐藏 |
| 启用通知提示音 | 弹出提醒浮层时播放气泡音 |
| 启用休息结束提示音 | 休息自然结束时播放提示音 |
| 全屏时自动勿扰 | 前台应用全屏（游戏/视频）时自动进入勿扰并继续计时；退出后恢复，若已到休息时间会立即提醒 |
| 记录浏览器数据（仅域名） | **默认关闭**。开启后左栏出现「浏览器」页签，开始按**网站域名**统计浏览器使用时长；只记录域名（如 `baidu.com`），**不记录完整网址与页面标题**。关闭后立即停止记录并隐藏页签，已有数据保留 |

设置点「应用」后即时生效并保存。

---

## 七、数据与隐私

- 所有数据**仅保存在本地** `user_data\` 目录，不上传任何服务器。
  - `config.json` — 配置
  - `events\`、`minute_usage\` — 使用记录
  - `minute_domains\` — 浏览器域名统计（仅开启「记录浏览器数据」后产生）
  - `app_icons\`、`domain_icons\` — 缓存的应用图标 / 网站图标
  - `debug.log` — 运行日志
- **浏览器记录的隐私设计**：程序通过系统无障碍接口读取浏览器地址栏（不装浏览器插件、不读浏览历史文件），读到后**立即只提取域名**——完整网址、页面标题、搜索词都不会被保存或写入日志；地址栏正在输入时会跳过采集。获取网站图标时仅向该网站本身发起请求（不经第三方服务）。
- 备份/迁移：用工具栏「导出数据」打包，或直接复制整个 `user_data\` 目录。
- 通过托盘「打开数据目录」可快速定位。

---

## 八、常见问题

**Q：某个应用没有图标，只显示首字母？**
A：少数应用（如部分 Electron 打包应用）的可执行文件无法被系统提取图标，会自动回退为首字母方块，不影响计时。

**Q：改了休息时长，但「立刻休息」仍是旧时长？**
A：请确认在设置里点了「应用」。修改即时生效，再次休息即用新值。

**Q：关了窗口程序就没了？**
A：在「设置 → 关闭按钮行为」选择「最小化到托盘」，关闭后程序会留在托盘继续计时。

**Q：开了「记录浏览器数据」但浏览器页签里没有数据？**
A：确认设置里点了「应用」，然后把浏览器窗口放在前台正常浏览网页（`chrome://` 等内部页、新标签页不计）。光标停在地址栏里时会暂停采集，点一下网页内容区即可。支持 Chrome / Edge / Brave / Vivaldi / Opera（Firefox 尽力支持）。从源码运行需已安装 `comtypes` 依赖（重跑 `scripts\install_deps.bat`）。

**Q：能把某个子站点单独统计吗？比如把 Gmail 从 Google 里拆出来？**
A：能。打开该站点的详情页（应用设置 → 「网站」页签点站点，或左栏浏览器视图直接点域名卡片），在子站点列表勾选**「独立统计」**，该子站点立即成为独立条目，历史数据同步回溯拆分；要并回去，进入该独立站点自己的详情页取消勾选即可。详情页里还可以为站点设置**显示名**（如把 `mail.google.com` 显示为「Gmail」）。默认已把 Google 的 drive / photos / mail 三个拆开。

**Q：浏览器统计会记录我访问的具体网页吗？**
A：不会。只记录**域名**和使用时长（如 `bilibili.com` 用了 40 分钟），完整网址、页面标题、搜索内容一概不采集（详见第七节）。

**Q：数据会丢吗 / 换电脑怎么迁移？**
A：复制 `user_data\` 目录到新机器同位置即可；或用「导出/导入」。
